{
  "name": "complens-oauth-audit",
  "version": "2.0.0",
  "description": "OAuth App Audit - See every third-party app with access to your M365 or Google Workspace",
  "private": true,
  "scripts": {
    "build": "sam build",
    "deploy:dev": "sam deploy --config-env dev",
    "deploy:prod": "sam deploy --config-env prod",
    "local": "sam local start-api --env-vars env.json",
    "test": "jest",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.55.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
